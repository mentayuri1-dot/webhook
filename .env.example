# Webhook Redirector Configuration

# URL to redirect incoming requests to
REDIRECT_URL=http://localhost:8080/webhook

# Port for the webhook to listen on
LISTEN_PORT=5000

# Host to bind to (0.0.0.0 means accessible from outside localhost)
HOST=0.0.0.0

# Enable debug mode (True/False)
DEBUG=False